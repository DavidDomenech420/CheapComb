Para inicializar el proyecto poner el siguiente comando

```
npx expo start -c --tunnel
```

Para instalar un paquete con expo esta el siguiente comando

```
npx expo install [nombre-del-paquete]
```

